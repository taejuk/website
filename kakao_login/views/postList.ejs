<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf8">
    </head>
    <body>
        <table>
            <% for(var i=0; i < posts.length; i++) { %>
               <tr>
                <td><a href='/postList/<%= posts[i].board_id %>'><%= posts[i].title %></a></td>
               </tr>
            <% } %>
            </table>
    </body>
</html>