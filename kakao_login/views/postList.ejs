<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf8">
    </head>
    <body>
        <h3><%= community %></h3>
        <table>
            <% for(var i=0; i < posts.length; i++) { %>
               <tr>
                <td><a href='/listPost/<%= community %>/<%= posts[i].board_id %>'><%= posts[i].title %></a></td>
               </tr>
            <% } %>
            </table>
            <a href="/createPost/<%= community %>">글 작성하기</a>
    </body>
</html>